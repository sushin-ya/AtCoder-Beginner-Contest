#include <bits/stdc++.h>
using namespace std;

int main() {
  int H, W;
  cin >> H >> W;
  vector<string> C(H);
  for (int i = 0; i < H; i++) cin >> C.at(i);
  for (int i = 0; i < H; i++) {
    cout << C.at(i) << endl;
    cout << C.at(i) << endl;
  }
}